#include <stdio.h>
#include "library.h"
//#define N 80

int rules(){
	//char str[N];
	//FILE *f;
	//f=fopen("rules/rules.txt","r");
	//if (fgets(str,N,f)==NULL)
	//return 0;
	//while(fgets(str,N,f)!=NULL)
		//printf("%s",str);
		//printf("\n");
		//fclose(f);
return 1;
}
